import { FieldTemplateProps } from "@rjsf/utils";

export const FormControlOnlyFieldTemplate = (props: FieldTemplateProps) => {

    return (
    <>
        {props.children}
    </>
    );

}
export default FormControlOnlyFieldTemplate;